"""
Shared test configuration for courtaccess/core/tests.

Add fixtures here that are shared across multiple test files in this package.
Module-level env var defaults belong in the source (os.getenv("KEY", "default")),
not here — conftest.py should only supply values that tests genuinely need and
that have no reasonable source-level default.
"""
