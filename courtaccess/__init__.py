"""
courtaccess — CourtAccess AI Python package.

Sub-packages:
  core/       — shared ML pipeline modules (OCR, translation, legal review, reconstruct)
  forms/      — form catalog management and scraping
  speech/     — real-time speech pipeline (VAD, ASR, TTS, session management)
  monitoring/ — drift detection, anomaly detection, bias analysis
"""
