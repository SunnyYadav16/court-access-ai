# ==============================================================================
# .gitignore — court-access-ai (monorepo root)
# Single consolidated file. Do NOT add nested .gitignore files anywhere.
# ==============================================================================


# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[codz]
*$py.class
*.so
*.egg
*.egg-info/
.installed.cfg
.eggs/
develop-eggs/
dist/
build/
MANIFEST
downloads/
eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
__pypackages__/

# ── PyInstaller ───────────────────────────────────────────────────────────────
*.manifest
*.spec

# ── Installer logs ────────────────────────────────────────────────────────────
pip-log.txt
pip-delete-this-directory.txt

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/
.Python
.pixi/
.pdm-python
.pdm-build/

# ── Tests & coverage ─────────────────────────────────────────────────────────
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
nosetests.xml
*.cover
*.py.cover
.hypothesis/
cover/

# ── Type checkers & linters ───────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
cython_debug/

# ── Notebooks ────────────────────────────────────────────────────────────────
.ipynb_checkpoints
profile_default/
ipython_config.py

# ── Secrets & environment ─────────────────────────────────────────────────────
.env
.envrc
!.env.example
!data_pipeline/.env.example

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.spyderproject
.spyproject
.ropeproject
.abstra/
.cursorignore
.cursorindexingignore

# ── Gemini AI agent artifacts ─────────────────────────────────────────────────
.gemini/

# ── Misc Python tools ────────────────────────────────────────────────────────
.scrapy
.pybuilder/
target/
/site
.pypirc
celerybeat-schedule
celerybeat.pid
*.sage.py
*.mo
*.pot
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
instance/
.webassets-cache
docs/_build/
marimo/_static/
marimo/_lsp/
__marimo__/


# ══════════════════════════════════════════════════════════════════════════════
# data_pipeline/
# ══════════════════════════════════════════════════════════════════════════════

# ── Airflow runtime directories (bind-mounted in Docker, never committed) ─────
data_pipeline/logs/
data_pipeline/plugins/
data_pipeline/config/
data_pipeline/airflow/

# ── DVC — local remote storage (binary cache, NOT tracked by Git) ─────────────
data_pipeline/dvc_storage/

# ── DVC — internal directories (absorbed from data_pipeline/.dvc/.gitignore) ──
data_pipeline/.dvc/tmp/
data_pipeline/.dvc/cache/
data_pipeline/.dvc/config.local

# ── DVC — tracked data files ──────────────────────────────────────────────────
# The raw data files are versioned in dvc_storage via `dvc push`.
# Only the small .dvc pointer files are committed to Git.
data_pipeline/dags/data/form_catalog.json

# ── Airflow-generated runtime reports (ephemeral, regenerated each run) ───────
data_pipeline/dags/data/anomaly_report.json
data_pipeline/dags/data/preprocess_report.json
data_pipeline/dags/data/bias_report.json
data_pipeline/dags/data/prev_catalog_metrics.json

# ── Downloaded & translated PDFs (DVC-tracked, not Git) ──────────────────────
data_pipeline/forms/

# ── Docker artifacts ──────────────────────────────────────────────────────────
*.tar
